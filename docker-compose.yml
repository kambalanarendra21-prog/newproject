services:
  postmaster:
    build: .
    ports:
      - "8080:8080"
    env_file:
      - .env
    environment:
      - DATA_DIR=/data
      - GOOGLE_CREDENTIALS_FILE=/data/secrets/credentials.json
      - GOOGLE_TOKEN_FILE=/data/secrets/token.json
      - GOOGLE_POSTMASTER_TOKEN_FILE=/data/secrets/token_postmaster.json
    volumes:
      - ./data:/data
    restart: unless-stopped
